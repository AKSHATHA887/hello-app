import routes from '../../src/routes';

describe('routes', () => {
  it('works', async () => {
    // test just to confirm routes are exportable
    expect(routes).toBeTruthy();
    // arrange
    // act
    // assert
  });
});
